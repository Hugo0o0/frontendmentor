import HeaderLogo from "./HeaderLogo/HeaderLogo";

const Header = () => {
  return (
    <header className="fixed top-0 left-0 w-full h-[96px] bg-item">
      <HeaderLogo />
    </header>
  );
};

export default Header;
